{
  "name": "relink-server",
  "version": "1.0.0",
  "description": "HTTP stream relink server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
